##  fixed some bugs and started writing the code for askllm 
* `Promises.all` is used for the  list of executions done while checking the array of folders if they were a valid git repo or not.
* `.env` file is a untracked file which is accessed using  `dotenv` 
* learnt some stuffs like about the `POST` method and processsing the `API KEY` while writing the code for llm call .
