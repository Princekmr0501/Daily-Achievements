## working with Databases  
* started a database on my local machine and made a table named food inisde the local's machine database 
* dotenv is used to read the `.env` file and prisma automatically reads `.env` without using dotenv