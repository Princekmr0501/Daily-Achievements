## completed the `askllm` function
* completd the `askllm function`
* `.env` file contains private information and should be inside the folders which would have to inject the `.env` data.
* `ES modules` uses import functions while cleaner js versions uses `require`.
* `choices[0]` means select he first response generated as llm generates various responses
* `tree` is a git command that shows the structure of the project .

