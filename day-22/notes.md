## Learnt about how to send and recieve the data from AI 
* made a try - catch error condition for sending the response and catching the error if `llm` failed to response .
* learnt about the following terms :
* `module.exports = router` helps to send the data out from a specific file so oother people can use it
* `require("dotenv").config()` through this we can access the `.env` file which is not meant 
to be tracked.