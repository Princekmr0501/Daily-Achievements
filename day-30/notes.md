## Runnig  all the servers and checking all the routes.
* imported `import { healthRouter } from './routes/health.route.ts'` inside app.ts
* `nvm` is a tool that helps us to switch between different  versions of `Node.js`
*  for i.e `backend/
 └── src/
     └── routes/
         └── analyze.route.ts`  
    From analyze.route.ts:
* ../ → routes → src
* ../../ → src → backend
* ./../../ → backend → one more level above backend
* `noEmit` chnges `ts` to `js` files and it is enabled true while using 'ts-node'.

