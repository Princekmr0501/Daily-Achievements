##  What is Eslint and the use of it .
* Eslint is a tool that helps users to optimize the code and make the code lool prettier and   well structured.
* A project leader can make  the rules using Eslint if he wants that these rules should be used by the members of the team working on the project.
* went through  and learnt about different types of errors and warning while installing Eslint. 
* `rm --cached filename` is used to untrack the commited files.
* `.gitignore` is used to always untracks the files inside it(whether it's used by any user).
* learnt about 
* `break` can also be used inside `if`,if it's inside a `case`;
*  `return`  statement should always be inside a function
* learnt to use rules according to the user's convienience.
* Made the rules in the Eslint like :
* There should be 4 indent spaces  and no semicolons inside the code 