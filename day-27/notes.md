## Installation of Typescript and optimization of code from js to ts
* learnt about the structure that how `src` and `dist` are used 
* `src` helps to keep the typescripts code and file safe.
* `dist` contains the file which is compiled from js to ts.
* optimized the `js` codes to `ts`codes for a safer and better code .
* learnt about some keywords like `string`,`number`,`readonly`,`unknown`,`any`,`Application` and other keywords .
